datasource:
  driver: com.mysql.jdbc.Driver
  url: jdbc:mysql://192.168.50.40:3306/dbs?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
  username: root
  password: 813929MySQL
# 自定义jwt key
jwt:
  tokenHeader: Authorization #JWT存储的请求头
  secret: mySecret #JWT加解密使用的密钥
  expiration: 604800 #JWT的超期限时间(60*60*24)
  tokenHead: Bearer  #JWT负载中拿到开头
file:
  path: /home/stephen/Downloads

logging:
  file:
    name: src/main/resources/sqlope.log
#mybatis:
#  mapper-locations: classpath*:me/stephenj/sqlope/dao/*.xml
#  type-aliases-package: me.stephenj.sqlope.dao